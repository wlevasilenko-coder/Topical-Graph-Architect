# Migration notes v4.0.7 -> v4.0.8

- v4.0.8 adds provisional_query_groups and latent_pattern_reconciliation to the output.
- If legacy consumers use JSON schema with `additionalProperties: false`, update schema or implement version-aware routing.
- Review any consumers that expect exact enum values for module states and update accordingly.
